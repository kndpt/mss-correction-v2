import { ManualOrderView } from 'src/sections/manual-order/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Créer une commande manuelle',
};

export default function ManualOrderPage() {
  return <ManualOrderView />;
}
